<UserControl x:Class="WPF_utils.Views.AliasManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Warning Banner -->
        <Border x:Name="BannerPathWarning" Grid.Row="0" Background="#FFF9C4" BorderBrush="#FFF59D" BorderThickness="1" CornerRadius="4" Margin="10,10,10,0" Padding="10" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon Kind="AlertOutline" Width="24" Height="24" Foreground="#F57F17" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBlock x:Name="txtWarningMessage" Grid.Column="1" TextWrapping="Wrap" Foreground="#333333" VerticalAlignment="Center" />

                <Button Grid.Column="2" Content="Add to PATH automatically" Click="AddToPath_Click" Margin="10,0,10,0" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#F57F17" />

                <Button Grid.Column="3" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="30" Height="30" Click="DismissWarning_Click">
                    <materialDesign:PackIcon Kind="Close" Foreground="#333333" />
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel: List -->
            <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}" CornerRadius="4" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="My Aliases" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,10" />

                    <ListView x:Name="lvAliases" Grid.Row="1" SelectionChanged="lvAliases_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding AliasName}" FontWeight="Bold" />
                                    <Run Text=" &#x2192; " Foreground="Gray" />
                                    <Run Text="{Binding Command}" Foreground="Gray" />
                                </TextBlock>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Button Grid.Row="2" Content="Add New Alias" Margin="0,10,0,0" Click="AddAlias_Click" Style="{StaticResource MaterialDesignOutlinedButton}" />
                </Grid>
            </Border>

            <!-- Right Panel: Edit Form -->
            <Border Grid.Column="2" Background="{DynamicResource MaterialDesignPaper}" CornerRadius="4" Padding="20">
                <StackPanel>
                    <TextBlock x:Name="txtFormTitle" Text="Create Alias" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20" />

                    <TextBox x:Name="txtAliasName" materialDesign:HintAssist.Hint="Alias Name (e.g. antide)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,20" />

                    <TextBox x:Name="txtCommand" materialDesign:HintAssist.Hint="Command to Execute (e.g. antigravity-ide %*)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,20" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />

                    <TextBlock x:Name="txtStatus" Foreground="Red" Margin="0,0,0,20" TextWrapping="Wrap" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnDelete" Content="Delete" Click="DeleteAlias_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="Red" BorderBrush="Red" Margin="0,0,10,0" />
                        <Button x:Name="btnSave" Content="Save Alias" Click="SaveAlias_Click" Width="150" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>